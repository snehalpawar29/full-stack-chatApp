apiVersion: v1
kind: Service
metadata:
  name: backend-service
  namespace: chatapp-ns
spec:
  selector:
    app: chatapp-backend
  ports:
    - protocol: TCP
      port: 5001
      targetPort: 5001

